# Environment Variables Template
# Copy this file to .env.local and fill in the values

# VITE_API_URL - Vercel proxy URL (preferred for production)
# When set, frontend uses /api/fetch on this URL as proxy to Railway backend
VITE_API_URL=https://aura-downloader.vercel.app

# VITE_BACKEND_URL - Direct Railway backend URL (fallback)
# Used when Vercel proxy is not available
VITE_BACKEND_URL=https://aura-downloader-production.up.railway.app

# BACKEND_URL - Used by api/fetch.ts on Vercel to proxy requests
# This is added to Vercel Dashboard Settings -> Environment Variables
BACKEND_URL=https://aura-downloader-production.up.railway.app

# Optional: Gemini API key for AI features
# Get this from https://aistudio.google.com/
# GEMINI_API_KEY=your-gemini-api-key
