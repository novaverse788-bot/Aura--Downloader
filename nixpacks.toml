# Nixpacks configuration for Railway deployment
# Ensures Python runtime is available alongside Node.js detection

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "python api/main.py"
